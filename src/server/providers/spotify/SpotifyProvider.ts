export class SpotifyProvider{get mode(){return process.env.SPOTIFY_CLIENT_ID&&process.env.SPOTIFY_CLIENT_SECRET?'spotify':'manual'}authorizationUrl(state:string){const p=new URLSearchParams({client_id:process.env.SPOTIFY_CLIENT_ID??'',response_type:'code',redirect_uri:process.env.SPOTIFY_REDIRECT_URI??'http://127.0.0.1:3000/api/spotify/callback',scope:'user-read-playback-state user-modify-playback-state',state});return `https://accounts.spotify.com/authorize?${p}`}async refresh(refreshToken:string){const auth=Buffer.from(`${process.env.SPOTIFY_CLIENT_ID}:${process.env.SPOTIFY_CLIENT_SECRET}`).toString('base64');const response=await fetch('https://accounts.spotify.com/api/token',{method:'POST',headers:{Authorization:`Basic ${auth}`,'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({grant_type:'refresh_token',refresh_token:refreshToken})});if(!response.ok)throw new Error('Spotify token refresh failed');return response.json() as Promise<{access_token:string;expires_in:number}>}}
